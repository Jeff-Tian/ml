tests/test_imagenet.c内容如下：

#include "unity.h"

extern void load_target(); // 假设load_target是你要测试的函数

void test_load_target() {
    // 在这里添加针对load_target的测试用例
    // 例如，调用load_target并验证其输出
}

int main() {
    UNITY_BEGIN();
    RUN_TEST(test_load_target);
    return UNITY_END();
}

Makefile (修改)内容如下：

CC = gcc
CFLAGS = -Iunity
TEST_SRC = tests/test_imagenet.c
UNITY_SRC = tests/unity/unity.c
TARGET = test_imagenet

all: $(TARGET)

$(TARGET): $(TEST_SRC) $(UNITY_SRC)
	$(CC) $(CFLAGS) -o $(TARGET) $(TEST_SRC) $(UNITY_SRC)

clean:
	rm -f $(TARGET)